\section{Design og arkitektur}



Dette kapittelet beskriver det tekniske oppsettet av prototypen -- hvilke teknologier
som er valgt, hvordan infrastrukturen er organisert, og hvordan endringer rulles ut
automatisk. Selve søke- og navigasjonsløsningene som er bygget oppå denne arkitekturen
beskrives i kapitlene~6 og~7.

\subsection{Teknologistack}

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\textwidth]{Images/Figures/helsedirAiTechStack}
    \caption{Oversikt over teknologistacken som er benyttet i prototypen.}
    \label{fig:tech-stack}
\end{figure}

Figur~\ref{fig:tech-stack} viser en oversikt over de ulike komponentene i systemet.
\hyperlink{ord:frontend}{Frontend} er bygget med React~\cite{reactjs-docs} og TypeScript
og bruker Vite som byggverktøy. Grensesnittet følger Digdirs designsystem og kommuniserer
med \hyperlink{ord:backend}{backend} via et RESTful-API (Representational State Transfer).

\hyperlink{ord:backend}{Backend} er implementert i Python med
FastAPI-\hyperlink{ord:rammeverk}{rammeverket}~\cite{fastapi-docs}, et moderne
asynkront rammeverk som automatisk genererer API-dokumentasjon. Backend håndterer
søkespørringer, innhenting av innhold og logging av brukeratferd. Den er koblet til en
MySQL~8-database der innhold, søkelogg og klikkstatistikk lagres.

\paragraph{Helsedirektoratets API (HAPI)} er et eksternt REST-grensesnitt som gir
tilgang til faglig innhold publisert av Helsedirektoratet~\cite{helsedir-api-docs}.
Innholdet er strukturert etter type -- blant annet faglige anbefalinger, retningslinjer
og temautdrag. I prototypen brukes HAPI på to måter:

\begin{itemize}
    \item Frontend henter utdypende detaljfelter (f.eks.\ begrunnelse og
        styrkegradering for anbefalinger) direkte fra HAPI for innhold som ikke
        er fullstendig lagret i den lokale databasen.
    \item Backend tilbyr et \hyperlink{ord:proxy}{proxy}-endepunkt mot HAPI slik at
        API-nøkkelen ikke eksponeres i nettleseren.
\end{itemize}

\subsection{Frontend}

\begin{figure}[H]
    \centering
    \includesvg[width=1.2\textwidth]{Images/frontendTech}
    \caption{Oversikt over frontend-arkitekturen med teknologier fordelt i lag.}
    \label{fig:frontend-tech-stack}
\end{figure}

Figur~\ref{fig:frontend-tech-stack} viser frontenden organisert i funksjonelle lag.
Komponentlaget er bygget med React og følger Digdirs designsystem, som er standard for
digitale tjenester i norsk offentlig sektor. Dette sikrer visuell konsistens og ivaretar
tilgjengelighetskrav uten at teamet selv måtte designe et grunnleggende komponentsett.

Navigasjon mellom sider håndteres av React~Router, som gir klient-side ruting uten full
sideopplasting. Data fra backend-APIet hentes og caches med TanStack~Query, som
automatisk håndterer bakgrunnsoppdatering og skiller tydelig mellom server-state og
UI-logikk. Global klient-state som søke-ID og siste spørring persisteres på tvers av
ruter med Zustand. HTML-innhold fra HAPI saniteres av DOMPurify før rendering for
å hindre XSS (cross-site scripting).

\subsection{Backend}

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\textwidth]{Images/Figures/backend-figur}
    \caption{Oversikt over backend-arkitekturen med teknologier fordelt i lag.}
    \label{fig:backend-tech-stack}
\end{figure}

Figur~\ref{fig:backend-tech-stack} viser backend-arkitekturen organisert etter en lagdelt
struktur med tydelig separasjon av ansvar. FastAPI definerer HTTP-endepunkter og genererer
automatisk API-dokumentasjon (OpenAPI/Swagger), noe som forenkler integrasjon og testing.

Innkommende forespørsler koordineres i et controller-lag som delegerer til riktige services.
Service-laget inneholder forretningslogikken -- blant annet orkestrering av hybrid søk og
proxy-kall mot HAPI. Et eget repository-lag abstraherer all databasetilgang mot MySQL, slik
at SQL-logikken holdes adskilt fra resten av koden.

Denne arkitekturen ble valgt fordi den gir god testbarhet og gjør det enkelt å bytte ut
enkeltkomponenter -- for eksempel søkemodell eller database -- uten at det påvirker
resten av systemet.



\subsection{Infrastruktur}

Prototypen er distribuert på en virtuell maskin i NTNUs OpenStack-baserte
skytjeneste (SkyHigh), og er tilgjengelig via domenet:

\texttt{https://vm.idatg2901-v26-helsdir.iaas.iik.ntnu.no/}

Infrastrukturen er organisert i tre hovedlag: skyinfrastruktur i OpenStack,
kjøremiljø på virtuell maskin med containeriserte tjenester, samt et
applikasjons- og nettverkslag der Nginx fungerer som inngangspunkt for
ekstern trafikk. Formålet er å muliggjøre stabil og isolert drift av
frontend, backend og database på en kontrollert og sikker måte.

Figur~\ref{fig:vm-internal} gir en samlet oversikt over den interne
strukturen på VM-en, inkludert alle komponentene og kommunikasjonsflyten
mellom dem.

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\textwidth]{Images/Figures/VM-internal}
    \caption{Intern struktur på VM-en med Nginx, Docker-containere og CI/CD-komponenter.}
    \label{fig:vm-internal}
\end{figure}

\subsubsection{Nettverk og tilgjengelighet}

Den virtuelle maskinen er plassert i et privat nettverk
(\texttt{192.168.0.0/24}) opprettet i OpenStack. Nettverket er koblet
til det eksterne nettverket \textit{ntnu-global} via en virtuell ruter.
Instansen har intern IP-adresse \texttt{192.168.0.173} og er tilordnet
en flytende IP-adresse (\texttt{129.241.150.141}), som gjør den
tilgjengelig fra internett.

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\textwidth]{Images/Figures/network-infrastructure(2)}
    \caption{Trafikkflyt fra URL til virtuell maskin i OpenStack, inkludert
    DNS-oppløsning, flytende IP-adresse og ruting gjennom privat subnett.}
    \label{fig:infra-setup}
\end{figure}

Som vist i figur~\ref{fig:infra-setup} løses domenenavnet først
til den flytende IP-adressen via DNS. Deretter sendes HTTPS-trafikken
til det eksterne nettverket, videre gjennom den virtuelle ruteren
og inn til instansen i det private subnettet. Denne
arkitekturen følger et klassisk skyoppsett hvor instanser isoleres i
private nettverk, samtidig som de kan eksponeres selektivt mot
offentlige nettverk~\cite{nist-cloud}.

\subsubsection{Konfigurasjon i OpenStack}

For å gjøre løsningen tilgjengelig fra internett er det konfigurert et
privat nettverk, en virtuell ruter mot \textit{ntnu-global} og en
flytende IP-adresse i OpenStack. Instansen er tilknyttet en egen
security group som tillater innkommende trafikk på portene 22 (SSH),
80 (HTTP), 443 (HTTPS), 3306 (MySQL) og 8000 (backend).

Portene 3306 og 8000 er åpne for å muliggjøre ekstern administrasjon
og testing i utviklingsfasen. I en produksjonssetting burde disse vært
begrenset til interne nettverk eller fjernet helt for å redusere
muligheter for eksterne angrep.

\subsubsection{Virtuell maskin og lagring}

Løsningen kjøres på én virtuell maskin med Ubuntu Server 24.04, konfigurert med
6 virtuelle CPU-kjerner, 8~GB RAM og et separat SSD-basert blokkvolum på 256~GB
for vedvarende lagring av databasedata og modeller. VM-en fungerer som
applikasjonsserver og kjører webserver, backend-applikasjon og database i containere.
Det er ikke benyttet lastbalansering eller høytilgjengelighetsmekanismer -- en bevisst
avveiing mellom enkelhet og skalerbarhet, i tråd med prototypens formål.

Tilgang til maskinen skjer via SSH med nøkkelbasert autentisering.
Passordinnlogging er deaktivert, og kun forhåndsautoriserte offentlige
nøkler gir tilgang.

\subsubsection{Containerisering}

Containerisering med Docker ble valgt for å isolere applikasjonens
avhengigheter fra vertsmaskinen og sikre reproduserbar utrulling på
tvers av miljøer. Containere muliggjør lettvekts virtualisering basert
på operativsystemnivå, noe som gir lavere ressursbruk sammenlignet med
tradisjonelle virtuelle maskiner~\cite{pahl2015containers}.

Backend-applikasjonen og databasen kjøres derfor i separate
Docker-containere administrert med Docker Compose.

Sensitive konfigurasjonsverdier som databasepassord og tilkoblingsinformasjon håndteres gjennom miljøvariabler definert i en lokal \texttt{\detokenize{.env}}-fil. I Docker Compose refereres disse variablene eksplisitt via \texttt{\detokenize{env_file}} og \texttt{environment}, slik at hemmeligheter holdes adskilt fra kildekoden og ikke hardkodes i applikasjonen.

\subsubsection{Tjenestestruktur og oppstart}

Databasen benytter MySQL~8 og kjører i en egen container med vedvarende
lagring gjennom et navngitt Docker-volum. Databasen initialiseres
automatisk ved første oppstart gjennom et SQL-oppsettskript.

Databasecontaineren er konfigurert med en helsesjekk basert på
\texttt{mysqladmin ping}~\cite{docker-healthcheck}, som benyttes for
å verifisere at databasen er tilgjengelig før andre tjenester starter.
Restart-policy er satt til \texttt{unless-stopped} for å sikre automatisk
gjenoppretting ved uventede feil.

Backend-applikasjonen bygges fra prosjektets Dockerfile basert på
\texttt{python:3.11-slim} og kjøres med Uvicorn. Applikasjonen
eksponerer et \texttt{/health}-endepunkt som benyttes både av Docker
og av eksterne overvåkingsmekanismer.

Gjennom \texttt{depends\_on}-konfigurasjon i Docker Compose starter
ikke backend før databasen er bekreftet som \textit{healthy}~\cite{docker-compose-depends}.
Dette sikrer korrekt oppstartsrekkefølge og reduserer risiko for
feilsituasjoner ved omstart.

\subsubsection{Frontend-distribusjon}

Frontend kjøres ikke i container. Den bygges lokalt og distribueres
som statiske filer til \texttt{/var/www/app} på VM-en.
Dette reduserer kompleksiteten i deploy-prosessen og gjør at
Nginx kan servere frontend direkte uten mellomliggende applikasjonslag.

\subsubsection{Nginx som reverse proxy}

Nginx er installert direkte på VM-en og fungerer som inngangspunkt for
all ekstern trafikk. Bruken av en reverse proxy muliggjør sentralisert håndtering av
TLS-terminering, trafikkruting og tilgangskontroll.
En slik arkitektur reduserer direkte eksponering av backend-tjenester
mot internett og er et etablert mønster i moderne webapplikasjoner~\cite{cloudflare-reverse-proxy}.

HTTPS termineres i Nginx ved bruk av TLS (Transport Layer Security)-sertifikater
utstedt av Let's Encrypt. All HTTP-trafikk omdirigeres automatisk til HTTPS. TLS-terminering i reverse proxyen innebærer at kryptert trafikk
dekrypteres før den videresendes internt i systemet. Dette forenkler
sertifikathåndtering og reduserer kompleksitet i applikasjonslaget.
TLS-protokollen er en standard for sikker webkommunikasjon~\cite{rfc8446}.

Nginx serverer frontend som statiske filer fra
\texttt{/var/www/app}, mens forespørsler til stien \texttt{/api}
videresendes til backend-containeren på \texttt{127.0.0.1:8000}.
Kommunikasjonen mellom Nginx og backend foregår internt på VM-en via HTTP.

Denne arkitekturen gjør at frontend og backend fremstår som én samlet
tjeneste for klienten, samtidig som backend isoleres bak reverse proxyen.
Løsningen unngår dermed CORS-problemer (Cross-Origin Resource Sharing) og sentraliserer
håndtering av TLS og sikkerhetsinnstillinger.

\subsubsection{GitHub-integrasjon og CI/CD}

VM-en er konfigurert med en self-hosted GitHub Actions runner som muliggjør
automatisk bygging og utrulling av backend ved push til repositoryet.
Runneren kjører direkte på VM-en og har tilgang til Docker-miljøet,
slik at oppdatering av containere kan automatiseres.

Dette gir en enkel CI/CD-prosess der kodeendringer kan deployeres uten
manuell SSH-innlogging.

Infrastrukturoppsettet legger til rette for isolert, reproduserbar og sikker
drift av prototypen, og opprettholder tilstrekkelig enkelhet for utvikling og
testing. Det er ikke designet for høy tilgjengelighet eller horisontal
skalering, men representerer et hensiktsmessig kompromiss for prosjektets
omfang og formål.

\subsection{CI/CD med GitHub Actions}
%Få også inn secrets, ssh mellom runner og github

GitHub Actions~\cite{github-actions-docs} er brukt for å automatisere
kvalitetskontroll og utrulling (CI/CD -- Continuous Integration/Continuous Deployment).
Det er satt opp tre arbeidsflyter som til sammen utgjør en pipeline fra kodeendring til
kjørende produksjonstjeneste.

\paragraph{PR-byggsjekk} kjøres automatisk ved pull request mot
\texttt{dev}-grenen og kontrollerer at koden er fri for lint-feil og typeproblemer
før den merges, som vist i kode~\ref{kode:yaml-pr}:

\begin{lstlisting}[language=yaml,
    caption={GitHub Actions: PR-byggsjekk for frontend},
    label={kode:yaml-pr}]
name: PR Build Check
on:
  pull_request:
    branches: ["dev"]
jobs:
  prebuild-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20.19.0"
          cache: "npm"
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
\end{lstlisting}

\paragraph{Frontend-utrulling} kjøres automatisk når endringer merges til
\texttt{main}. Arbeidsflyten bygger applikasjonen, kopierer den til VM-en og
laster Nginx på nytt. Avslutningsvis kjøres en røyktest mot det offentlige
domenet for å bekrefte at tjenesten svarer, som vist i kode~\ref{kode:yaml-frontend}:

\begin{lstlisting}[language=yaml,
    caption={GitHub Actions: Utrulling av frontend},
    label={kode:yaml-frontend}]
name: Deploy frontend
on:
  push:
    branches: ["main"]
jobs:
  build-and-deploy:
    runs-on: [self-hosted, prod]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Build
        env:
          VITE_API_BASE_URL: /api
          VITE_HELSEDIR_API_KEY: ${{ secrets.VITE_HELSEDIR_API_KEY }}
        run: npm run build
      - run: sudo rsync -a --delete dist/ /var/www/app/
      - run: sudo nginx -t && sudo systemctl reload nginx
      - run: curl -fsS https://vm.idatg2901-v26-helsdir.iaas.iik.ntnu.no/api/health
\end{lstlisting}

\paragraph{Backend-utrulling} henter nyeste kode fra Git og bygger
Docker-containeren på nytt. Arbeidsflyten venter deretter på at helsesjekken
godkjennes -- noe som sikrer at en mislykket oppdatering oppdages umiddelbart,
som vist i kode~\ref{kode:yaml-backend}:

\begin{lstlisting}[language=yaml,
    caption={GitHub Actions: Utrulling av backend},
    label={kode:yaml-backend}]
name: Deploy backend (prod)
on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: [self-hosted, prod]
    steps:
      - name: Deploy on VM
        run: |
          cd /home/ubuntu/helsedir-ai-backend
          git fetch origin
          git reset --hard origin/main
          docker compose build backend
          docker compose up -d --no-deps backend
          for i in {1..100}; do
            status=$(docker inspect \
              -f '{{.State.Health.Status}}' helsedir-ai-backend)
            [ "$status" = "healthy" ] && exit 0
            [ "$status" = "unhealthy" ] && exit 1
            sleep 3
          done
\end{lstlisting}

Alle tre arbeidsflytene kjøres på en selvhostet GitHub Actions-runner
(\texttt{self-hosted, prod}) som er installert direkte på VM-en. Dette gjør at
utrulling skjer uten å sende kode over nettverket -- runneren har allerede tilgang
til det lokale filsystemet og Docker-miljøet på maskinen.

